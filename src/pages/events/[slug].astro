---
// The events detail template, nativised (REBUILD-PLAN.md Phase 4 step 3, shape
// 923a59d8 -- shared across 8 collections, see GenericDetailPage.astro's doc
// comment for why the whole group reduces to one shared component with zero
// per-item bindings). DetailPage/render-detail.mjs and the events shell+bindings
// stay in the tree -- other collections still use that path -- but /events/<slug>
// no longer does.
import { getPaths } from '../../lib/content';
import GenericDetailPage from '../../components/detail-shared/GenericDetailPage.astro';

export const getStaticPaths = () => getPaths('events');
---
<GenericDetailPage collection="events" wfPageId="649aa8a9681ec6168a57d94f" />
