---
// The faqs detail template, nativised (REBUILD-PLAN.md Phase 4 step 3, shape
// 923a59d8 -- see GenericDetailPage.astro's doc comment). DetailPage/
// render-detail.mjs and the faqs shell+bindings stay in the tree -- other
// collections still use that path -- but /faqs/<slug> no longer does.
import { getPaths } from '../../lib/content';
import GenericDetailPage from '../../components/detail-shared/GenericDetailPage.astro';

export const getStaticPaths = () => getPaths('faqs');
---
<GenericDetailPage collection="faqs" wfPageId="649aa8a9681ec6168a57d950" />
