---
// The full-stack-social-comments detail template, nativised (REBUILD-PLAN.md
// Phase 4 step 3, shape 923a59d8 -- see GenericDetailPage.astro's doc comment).
// DetailPage/render-detail.mjs and the full-stack-social-comments shell+bindings
// stay in the tree -- other collections still use that path -- but
// /full-stack-social-comments/<slug> no longer does.
import { getPaths } from '../../lib/content';
import GenericDetailPage from '../../components/detail-shared/GenericDetailPage.astro';

export const getStaticPaths = () => getPaths('full-stack-social-comments');
const { item } = Astro.props;
---
<GenericDetailPage collection="full-stack-social-comments" wfPageId="649aa8a9681ec6168a57d965" slug={item.fieldData.slug as string} />
