---
// The project-categories detail template, nativised (REBUILD-PLAN.md Phase 4
// step 3, shape 923a59d8 -- see GenericDetailPage.astro's doc comment).
// DetailPage/render-detail.mjs and the project-categories shell+bindings stay in
// the tree -- other collections still use that path -- but
// /project-categories/<slug> no longer does.
import { getPaths } from '../../lib/content';
import GenericDetailPage from '../../components/detail-shared/GenericDetailPage.astro';

export const getStaticPaths = () => getPaths('project-categories');
---
<GenericDetailPage collection="project-categories" wfPageId="66dafcf81128dd159338e35a" />
