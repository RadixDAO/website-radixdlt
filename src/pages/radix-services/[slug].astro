---
// The radix-services detail template, nativised (REBUILD-PLAN.md Phase 4 step 3,
// shape 923a59d8 -- see GenericDetailPage.astro's doc comment). DetailPage/
// render-detail.mjs and the radix-services shell+bindings stay in the tree --
// other collections still use that path -- but /radix-services/<slug> no longer
// does.
import { getPaths } from '../../lib/content';
import GenericDetailPage from '../../components/detail-shared/GenericDetailPage.astro';

export const getStaticPaths = () => getPaths('radix-services');
---
<GenericDetailPage collection="radix-services" wfPageId="65006fb5f3dde67a65a4e071" />
