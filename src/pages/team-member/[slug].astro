---
// The team-member detail template, nativised (REBUILD-PLAN.md Phase 4 step 3,
// shape 923a59d8 -- see GenericDetailPage.astro's doc comment). DetailPage/
// render-detail.mjs and the team-member shell+bindings stay in the tree -- other
// collections still use that path -- but /team-member/<slug> no longer does.
import { getPaths } from '../../lib/content';
import GenericDetailPage from '../../components/detail-shared/GenericDetailPage.astro';

export const getStaticPaths = () => getPaths('team-member');
---
<GenericDetailPage collection="team-member" wfPageId="649aa8a9681ec6168a57d967" />
