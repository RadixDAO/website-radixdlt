node_modules/
dist/
.astro/
.wrangler/
.env
.DS_Store
webflow.json

# ---------------------------------------------------------------------------
# What is NOT ignored, and why
# ---------------------------------------------------------------------------
# public/assets/         the 913 MB Webflow CDN mirror: 2,226 files reachable only from
#                         CMS rich text. Committed rather than parked in object storage --
#                         it is a frozen migration snapshot that will never be rewritten,
#                         so git never pays the binary-delta cost, and `git clone` stays
#                         the single restore path. See REBUILD-PLAN.md 0.2.
# source/webflow-export/  the Webflow static export, committed in full (241 MB).
#                         It dies with the Webflow subscription and is the input to
#                         every converter. Nothing here is regenerable.
# reference/live/         1,209-page snapshot of the live site: the verification
#                         oracle. Cannot be recaptured once Webflow stops serving.
# reference/webflow/      CMS pull: 29 collections, 1,590 items, field schemas.
# src/shells/             generated markup, but committed -- a fresh clone must build
#                         without re-running the converter. `pnpm convert` regenerates.

# NOTE: public/{css,js,fonts,images,videos,documents} are TRACKED. They used to be
# gitignored and regenerated from source/webflow-export/, but that export now lives in
# the migration archive, so deriving them would make a clone of this repo depend on
# another repo just to have stylesheets. They are the site's own assets -- radix-web.css
# is the design -- so they are committed like any other asset.

reference/mirror-live.log
reference/live-failures.txt
